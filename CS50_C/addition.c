// Addig two numbers

#include <stdio.h>
int main(void) {    

    int x, y, sum;
    printf("Enter two integers:\n ");
    scanf("%d %d", &x, &y);
    sum = x + y;      
    printf("x = %d + y = %d, sum = %d", x, y, sum);

}